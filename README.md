# prividium-examples
## Safe tx compose deployment

Use `.env.example` + `docker-compose.yml` to launch DB, one-shot init deployer, API, and optional UI profile (`--profile ui`). See `examples/safe-tx-service/README.md` for details.
